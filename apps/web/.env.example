# OntDekker Web — Environment Variables
#
# Copy this file to .env.local for local development.
# Never commit .env.local to version control.

# Base URL for the Traefik API gateway (no trailing slash).
# In production this will be the gateway domain.
# In local Docker dev this is http://localhost:80
NEXT_PUBLIC_API_BASE_URL=http://localhost:80
